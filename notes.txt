1. Explain the differences betwenn using claude as a human vs through APIs
2. Python + FastAPI
Configure API key
Claude API call from Python
Differences between application instructions vs user provided data


By end of day

Postman -> FastAPI -> anthropic messages API -> JSON RESPONSE

Day 1: API -> Prompt -> Structured Output -? Backend
Day 2: Database
Day 3: Security, performance


BIG PICTURE
OurAPP -> HTTP/API Request -> Claude API -> response -> OurAPP

REQUEST
client.messages.create(
    model:...
    system=".."
    messages=[
        {
            "role": "user,
            "content": "Summarise this... "
        }
    ]
)

MODEL
model="some-claude-model"

SYSTEM/APPLICATION Instructions
tells Calude how your app want it to behave
"
You are a customer support assistant
Summarise customer complaints objectively
Do not invent missing info
Return no more than 3 sentences
"

user content: "MY package was supposed to arrrive yesterday..."

SYSTEM: (app config/ behaviour rules)
"What job are we doing and what rules apply?"
USER MESSAGE: (runtime input)
"What am I asking Claude to process right now"

MESSAGES
input provieded to Claude
messages=[
    {
        "role": "user,
        "content": "Summarise this... "
    }
]

max_tokens=200 (upper bound, not target length)
DOES NOT CONTROL size of the incoming request

INPUT SIZE                  OUTPUT LIMIT    
messages + system ------>   claude ------> <= max_tokens


response.content
App -> Claude
Claude -> App

Characters -> Tokenizer -> Tokens -> Model

1. Capacity
2. Latency
3. Cost(amount processed + amount generated)

{
    "text": "100MB worth of text"
}

Client -> FastAPI/Pydantic (Will our app accept this much?) -> App (Do we need all this info) -> Claude (Does it fit the model/context contraint) -> max_output ()

class SummariseRequest(BaseModel):
    text: str = Field(length=5000)


What should or app do first?
Find relevant cust -> relevent order/ticker -> send only relevat context -> claude


This looks like a fairly serious shipping problem.
The customer sounds frustrated and probably needs someone to check the order.

{
  "summary": "Customer reports a repeated late-delivery issue.",
  "category": "delivery",
  "sentiment": "negative",
  "priority": "high",
  "needs_order_lookup": true,
  "needs_faq_lookup": false,
  "needs_human_review": false,
  "faq_query": null,
  "human_review_reason": null
}

customer messaage -> customerid + orderid -> OrderRepository -> OrderContext + customer message -> claude


question -> FAQREpo -> approved canditdates -> claude -> supported answer? -> app fallback

order lookup?
FAQ search?
ticket create
response generationo
update ticket


CLAUDE-ASSISTED
analyse tickets
draft responses

APPLICATION-CONTROLLED
validates request
retrieves order
retrieves faq
create/saving tickets


request -> analysis -> order and/or faq lookup -> create ticket -> generate reponse -> app decides final status -> update ticket

analyse request

if order lookup is required
    retrieve authorised order
    if no authorised order
        require human review

if faq lookup is required
    retrieve approved faq context
    if no aproved faq
        require human review

create ticket
generate response with trusted context
choose final status
update ticket
return result

Desterministic vs Agentic Workflows
DETERMINISTIC

Python chooses:
analyse
 -> retrieve
 -> persist
 -> generate
 -> update

AGENTIC
Application defines:
allowed capabilities + boundaries

Claude may request:
which permitted capability to use next

Application:
validates + authorizes + executes


Tool-use
Claude
  |
  | "I want get_order_status(...)"
  v
application
  |
  +--> is the tool allow listed?
  +--> do arguments validate?
  +--> is the requested order permitted?
  |
  v
repository / application function
  |
  v
tool_result
  |
  v
Claude

Claude decide next step -> tool_use -> ToolRegistry -> tool_result ->

ToolRegistry
    is this tool allowed?
    are the args valid?
    Is this order/customer permitted?
    Execute approved app code


    get_order_status(
        order_id="our_id"
    )

    Claude Code -> .claude
    Claude API -> FastAPI + AsyncAnthopic
    Managed agent / Agent runtime